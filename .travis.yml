language: julia
os:
  - linux
julia:
  - release
  - nightly
notifications:
  email: false
script:
  - julia --check-bounds=yes -e 'Pkg.clone(pwd()); Pkg.build("KernelEstimator"); Pkg.test("KernelEstimator"; coverage=true)'
